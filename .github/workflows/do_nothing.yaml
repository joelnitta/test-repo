name: No-Operation Workflow

on:
  push:
    branches:
      - main
      - l10n_main

jobs:
  do-nothing:
    runs-on: ubuntu-latest
    steps:
      - name: No Operation
        run: echo "Hello World!"
